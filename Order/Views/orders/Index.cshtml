@model IEnumerable<Order.Models.order>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.roll_no)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Eamil)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.roll_no)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Eamil)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.sudentid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.sudentid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.sudentid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
